export default function OptionsPage() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui, sans-serif", maxWidth: 480 }}>
      <h1>VaultLock — Extension Settings</h1>
      <p>This is a placeholder options page created during the initial Plasmo scaffold (12-01).</p>
      <p>
        Server URL configuration, auth state, and sync settings will be implemented in sub-task
        12-02.
      </p>
    </div>
  );
}
