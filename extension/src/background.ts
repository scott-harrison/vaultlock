/**
 * Background service worker for the VaultLock extension.
 *
 * This will eventually handle:
 * - Background sync with the server
 * - Message passing between popup, options, and content scripts
 * - Authentication token management
 *
 * For now (12-01) this is just a placeholder so the extension has a valid MV3 structure.
 */

console.log("[VaultLock] Background service worker initialized (scaffold)");
