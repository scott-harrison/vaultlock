# Non-interactive purge when pnpm must recreate node_modules (no TTY / CI).
confirm-modules-purge=false

# Stream progress instead of staying silent during long deletes and native builds.
reporter=append-only
