{
  "name": "vaultlock",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=24.0.0 <25.0.0",
    "pnpm": "11.5.2"
  },
  "scripts": {
    "prepare": "husky",
    "format": "biome format --write . && cargo fmt --all --manifest-path backend/Cargo.toml",
    "check:deps": "node scripts/check-pinned-deps.mjs",
    "lint": "biome lint . && node scripts/check-pinned-deps.mjs && cargo clippy --manifest-path backend/Cargo.toml --all-targets -- -D warnings",
    "lint:fix": "biome lint --apply . && cargo clippy --manifest-path backend/Cargo.toml --all-targets --fix --allow-dirty --allow-staged -- -D warnings",
    "test": "sh scripts/test-backend.sh && pnpm --filter @vaultlock/shared test",
    "desktop:dev": "pnpm --filter @vaultlock/desktop tauri dev",
    "desktop:build": "pnpm --filter @vaultlock/desktop tauri build",
    "extension:dev": "pnpm --filter @vaultlock/extension dev",
    "extension:dev:brave": "pnpm --filter @vaultlock/extension dev:brave",
    "extension:build": "pnpm --filter @vaultlock/extension build",
    "extension:build:brave": "pnpm --filter @vaultlock/extension build:brave",
    "extension:test": "pnpm --filter @vaultlock/extension test",
    "extension:test:e2e": "pnpm --filter @vaultlock/extension test:e2e",
    "ci": "biome check . && node scripts/check-pinned-deps.mjs && cargo fmt --all --manifest-path backend/Cargo.toml -- --check && cargo clippy --manifest-path backend/Cargo.toml --all-targets -- -D warnings"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "husky": "9.1.7",
    "typescript": "5.9.3"
  }
}
